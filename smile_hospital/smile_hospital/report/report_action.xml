<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ================= Treatment Report ================= -->
    <record id="action_report_treatment_pdf" model="ir.actions.report">
        <field name="name">Treatment Report</field>
        <field name="model">dental.prescription</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">smile_hospital.report_treatment_pdf</field>
        <field name="report_file">smile_hospital.report_treatment_pdf</field>
        <field name="print_report_name">'Treatment Report - %s' % ', '.join(object.treatment_id.mapped('name'))</field>
        <field name="binding_model_id" ref="smile_hospital.model_dental_prescription"/>
        <field name="binding_type">report</field>
    </record>

</odoo>
